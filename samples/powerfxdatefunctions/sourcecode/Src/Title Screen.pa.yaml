"'Title Screen' As screen":

    "fx_Date As 'Date Functions'":
        IsInNextNWeeks: |-
            =Date(
                Year(StartDate),
                Month(StartDate),
                Day(StartDate)
            ) <= EvaluateDate
            And
            DateAdd(
                Date(
                    Year(StartDate),
                    Month(StartDate),
                    Day(StartDate)
                )
                +Time(23,59,59),
                NumberOfWeeks*7,
                Days
            ) >= EvaluateDate
        IsInNextNQuarters: |-
            =Date(
                Year(StartDate),
                Month(StartDate),
                Day(StartDate)
            ) <= EvaluateDate
            And
            DateAdd(
                Date(
                    Year(StartDate),
                    Month(StartDate),
                    Day(StartDate)
                )
                +Time(23,59,59),
                NumberOfQuarters,
                Quarters
            ) >= EvaluateDate
        IsInPreviousNDays: |-
            =DateAdd(
                Date(
                    Year(StartDate),
                    Month(StartDate),
                    Day(StartDate)
                )
                ,
                -NumberOfDays,
                Days
            ) <= EvaluateDate
            And
            Date(
                Year(StartDate),
                Month(StartDate),
                Day(StartDate)
            ) +Time(23,59,59)
            >= EvaluateDate
        IsInPreviousNMonths: |-
            =DateAdd(
                Date(
                    Year(StartDate),
                    Month(StartDate),
                    Day(StartDate)
                )
                ,
                -NumberOfMonths,
                Months
            ) <= EvaluateDate
            And
            Date(
                Year(StartDate),
                Month(StartDate),
                Day(StartDate)
            ) +Time(23,59,59)
            >= EvaluateDate
        IsInPreviousNYears: |-
            =DateAdd(
                Date(
                    Year(StartDate),
                    Month(StartDate),
                    Day(StartDate)
                )
                ,
                -NumberOfYears,
                Years
            ) <= EvaluateDate
            And
            Date(
                Year(StartDate),
                Month(StartDate),
                Day(StartDate)
            ) +Time(23,59,59)
            >= EvaluateDate
        IsInPreviousNWeeks: |-
            =DateAdd(
                Date(
                    Year(StartDate),
                    Month(StartDate),
                    Day(StartDate)
                )
                ,
                -NumberOfWeeks*7,
                Days
            ) <= EvaluateDate
            And
            Date(
                Year(StartDate),
                Month(StartDate),
                Day(StartDate)
            ) +Time(23,59,59)
            >= EvaluateDate
        IsInPreviousNQuarters: |-
            =DateAdd(
                Date(
                    Year(StartDate),
                    Month(StartDate),
                    Day(StartDate)
                )
                ,
                -NumberOfQuarters,
                Quarters
            ) <= EvaluateDate
            And
            Date(
                Year(StartDate),
                Month(StartDate),
                Day(StartDate)
            ) +Time(23,59,59)
            >= EvaluateDate
        IsInNextNYears: |-
            =Date(
                Year(StartDate),
                Month(StartDate),
                Day(StartDate)
            ) <= EvaluateDate
            And
            DateAdd(
                Date(
                    Year(StartDate),
                    Month(StartDate),
                    Day(StartDate)
                )
                +Time(23,59,59),
                NumberOfYears,
                Months
            ) >= EvaluateDate
        IsInNextNMonths: |-
            =Date(
                Year(StartDate),
                Month(StartDate),
                Day(StartDate)
            ) <= EvaluateDate
            And
            DateAdd(
                Date(
                    Year(StartDate),
                    Month(StartDate),
                    Day(StartDate)
                )
                +Time(23,59,59),
                NumberOfMonths,
                Months
            ) >= EvaluateDate
        IsInNextNDays: |-
            =Date(
                Year(StartDate),
                Month(StartDate),
                Day(StartDate)
            ) <= EvaluateDate
            And
            DateAdd(
                Date(
                    Year(StartDate),
                    Month(StartDate),
                    Day(StartDate)
                )
                +Time(23,59,59),
                NumberOfDays,
                Days
            ) >= EvaluateDate
        IsInCurrentQuarter: |-
            =Date(
                Year(Today()),
                Month(Today())
                -If(
                    Mod(Month(Today())-1,3) = 0,
                    0,
                    Mod( Month(Today())-1, 3)
                ),
                1
            ) <= StartDate 
            And
            (
                Date(
                    Year(Today()),
                    Month(Today()) + 3-Mod(Month(Today())-1,3),
                    1
                )-1
            +Time(23,59,59))>= StartDate
        IsInCurrentWeek: |-
            =StartDate 
            - (Weekday(
                Today(),
                Switch(
                    DayWeekStart,
                    1,StartOfWeek.Sunday,
                    2,StartOfWeek.Monday,
                    3,StartOfWeek.Tuesday,
                    4,StartOfWeek.Wednesday,
                    5,StartOfWeek.Thursday,
                    6,StartOfWeek.Friday,
                    7,StartOfWeek.Saturday)
                )-1
            ) <= StartDate 
            And
            (
            DateAdd(
                StartDate,
                (7 - Weekday(
                    StartDate,
                    Switch(
                        DayWeekStart,
                        1,StartOfWeek.Sunday,
                        2,StartOfWeek.Monday,
                        3,StartOfWeek.Tuesday,
                        4,StartOfWeek.Wednesday,
                        5,StartOfWeek.Thursday,
                        6,StartOfWeek.Friday,
                        7,StartOfWeek.Saturday)
                    )
                ),
                Days
            )
            +Time(23,59,59)) >= StartDate
        IsInCurrentYear: |-
            =Date(
                Year(Today()),
                1,
                1
            ) <= StartDate 
            And
            (
                Date(
                    Year(Today()),
                    12,
                    31
                )
                + Time(23,59,59)
            ) >= StartDate
        IsInCurrentMonth: |-
            =Date(
                Year(Today()),
                Month(Today()),
                1
            ) <= StartDate 
            And
            (
                Date(
                    Year(Today()),
                    Month(Today()) + 1,
                    1
                ) - 1
                +Time(23,59,59)
            ) >= StartDate
        IsInCurrentDay: =IsToday(StartDate)
        WeekOfMonth: |-
            =With(
                {StartDate: Today(), DayWeekStart:1},
                Sum(
                    AddColumns(
                        ForAll(
                            Sequence(
                                DateDiff(
                                    Date(Year(StartDate),Month(StartDate),1),
                                    Date(Year(StartDate),Month(StartDate),Day(StartDate)),
                                    Days
                                )
                            ),
                            Date(Year(StartDate),1,1) + Value
                        ),
                        "IsWeekStart",
                        Weekday(Value)=DayWeekStart
                    ),
                    IsWeekStart
                )
            )
        StartOfQuarter: |-
            =DateAdd(
                Date(
                    Year(StartDate),
                    Month(StartDate)
                    -If(
                        Mod(Month(StartDate)-1,3) = 0,
                        0,
                        Mod( Month(StartDate)-1, 3)
                    ),
                    1
                ),
                AddQuarters,
                Quarters
            )
        EndOfQuarter: |-
            =DateAdd(
                Date(
                    Year(StartDate),
                    Month(StartDate) + 3-Mod(Month(StartDate)-1,3),
                    1
                )-1,
                AddQuarters,
                Quarters
            )
            +Time(23,59,59)
        YearFraction: |-
            =(
                DateDiff(
                    Switch(
                        Basis,
                        0, If(
                            Day(StartDate)=31,
                            Date(Year(StartDate), Month(StartDate), 30),
                            StartDate
                        ),
                        4, If(
                            Day(StartDate)=31,
                            Date(Year(StartDate), Month(StartDate), 30),
                            StartDate
                        ),
                        StartDate
                    ), 
                    Switch(
                        Basis,
                        0, If(
                            Date(Year(EndDate), Month(EndDate)+1, 1)-1=EndDate
                            And Day(StartDate)<30,
                            Date(Year(EndDate), Month(EndDate)+1,1),
                            Date(Year(EndDate), Month(EndDate)+1, 1)-1=EndDate
                            And Day(StartDate)>=30,
                            Date(Year(EndDate), Month(EndDate)+1, 1)-1,
                            EndDate
                        ),
                        4, If(
                            Day(EndDate)=31,
                            Date(Year(EndDate), Month(EndDate), 30),
                            EndDate
                        ),
                        EndDate
                    ), 
                    Days
                )-Switch(
                    Basis,
                    0, 1,
                    4, 1,
                    0
                )
            )
            /Switch(
                Basis,
                0, 360,
                1, DateDiff(Date(Year(EndDate),1,0), Date(Year(EndDate),12,31), Days),
                2, 360,
                3, 365,
                4, 360
            )
        NetWorkDays: |-
            =Sum(
                AddColumns(
                    ForAll(
                        Sequence(
                            DateDiff(
                                StartDate,
                                EndDate,
                                Days
                            )+1
                        ),
                        StartDate + Value
                    ),
                    "IsWeekday",
                    Weekday(Value) in [2,3,4,5,6]
                ),
                IsWeekday
            )
        WeekOfYear: |-
            =Sum(
                AddColumns(
                    ForAll(
                        Sequence(
                            DateDiff(
                                Date(Year(StartDate),1,1),
                                StartDate,
                                Days
                            )
                        ),
                        Date(Year(StartDate),1,1) + Value
                    ),
                    "IsWeekStart",
                    Weekday(Value)=DayWeekStart
                ),
                IsWeekStart
            )
        StartOfWeek: |-
            =StartDate 
            - (Weekday(
                StartDate,
                Switch(
                    DayWeekStart,
                    1,StartOfWeek.Sunday,
                    2,StartOfWeek.Monday,
                    3,StartOfWeek.Tuesday,
                    4,StartOfWeek.Wednesday,
                    5,StartOfWeek.Thursday,
                    6,StartOfWeek.Friday,
                    7,StartOfWeek.Saturday)
                )-1
            )+(7*AddWeeks)
        StartOfYear: =DateAdd(Date(Year(StartDate), 1, 1), AddYear)
        StartOfMonth: =DateAdd(Date(Year(StartDate), Month(StartDate), 1), AddMonths, Months)
        EndOfWeek: |-
            =DateAdd(
                StartDate,
                (7 - Weekday(
                    StartDate,
                    Switch(
                        DayWeekStart,
                        1,StartOfWeek.Sunday,
                        2,StartOfWeek.Monday,
                        3,StartOfWeek.Tuesday,
                        4,StartOfWeek.Wednesday,
                        5,StartOfWeek.Thursday,
                        6,StartOfWeek.Friday,
                        7,StartOfWeek.Saturday)
                    )
                )+(7*AddWeeks),
                Days
            )
            +Time(23,59,59)
        EndOfYear: |-
            =DateAdd(
                Date(
                    Year(StartDate),
                    12,
                    31
                ),
                AddYears,
                Years
            )
            +Time(23,59,59)
        EndOfMonth: |-
            =DateAdd(
                Date(
                    Year(StartDate),
                    Month(StartDate) + 1,
                    1
                ),
                AddMonths,
                Months
            ) - 1
            +Time(23,59,59)
        IsBetweenDates: |-
            =Date(
                Year(StartDate),
                Month(StartDate),
                Day(StartDate)
            ) <= EvaluateDate
            And (
            Date(
                Year(EndDate),
                Month(EndDate),
                Day(EndDate)
            ) +Time(23,59,59)
            ) >= EvaluateDate
        IsLeapYear: |-
            =With(
                {wCheckLeapYear: Date(Year(Today()), 2, 29)},
                Day(wCheckLeapYear) = 29 And Month(wCheckLeapYear) = 2
            )
        Width: =122
        Height: =123
        Y: =645
        X: =1244

    lbl_TItle As label:
        Text: ="Power Fx Date Functions Library"
        Align: =Center
        ZIndex: =3
        Height: =115
        Width: =1366
        Size: =48
        Y: =309
        Color: =RGBA(109, 49, 162, 1)

